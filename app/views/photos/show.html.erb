<div class="row">
  <div class="offset-md-3 col-md-5">
    <div style="width: 500px;" >
      <div class="card">
      <p>
        <%= image_tag(@photo.image.url, :class => "responsive-img", :height => "400", :width => "500") if @photo.image?%>
      </p>
      <div>クイズの投稿者：<%= link_to @photo.user.name, user_path(@photo.user) %></div>
      <p>
        <strong>クイズの問題:</strong>
        <%= @photo.content %>
      </p>
      
        <%= link_to "回答する", new_photo_answer_path(@photo) %>
        
        <%= render 'answers/answers', answer: @answers %>
      </div>
    </div>
      
      <%= paginate @answers %>
    </div>
</div>

<% if current_user == @photo.user %>
<%= link_to '編集', edit_photo_path(@photo) %> |
<% end %>
<%= link_to '戻る', root_path %> 
